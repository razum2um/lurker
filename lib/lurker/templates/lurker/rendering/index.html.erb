<div class="row">
    <div class="col-lg-12">
        <% if (content = @service_presenter.options[:content]).present? %>
          <%= raw content %>
        <% else %>
          <h1 class="page-header">Hi to Lurker!</h1>
        <% end %>
    </div>
    <!-- /.col-lg-12 -->
</div>
